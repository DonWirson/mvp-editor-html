import 'package:flutter/material.dart';

class HtmlPreview extends StatelessWidget {
  const HtmlPreview({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Función en desarrollo :D"),
    );
  }
}
